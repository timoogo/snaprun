/**
 * Placeholder value assigned to dynamic parameters for a newly discovered
 * route (RFC-006): the real value is not known at scan time. Single source
 * of truth; never duplicate this string elsewhere.
 */
export const PLACEHOLDER_PARAMETER_VALUE = "REPLACE_ME";
