/**
 * Valeur placeholder attribuée aux paramètres dynamiques d'une route
 * nouvellement découverte (RFC-006) : leur valeur réelle n'est pas connue au
 * moment du scan. Source unique — ne jamais dupliquer cette chaîne ailleurs.
 */
export const PLACEHOLDER_PARAMETER_VALUE = "REPLACE_ME";
